# 🎯 API 用量监控工具 - 新版本使用说明

## 📋 新版本特点

### ✅ 优点
1. **无需打开网页** - 完全静默运行
2. **无需浏览器** - 不需要安装Chrome
3. **执行更快** - 比旧版本快3倍
4. **资源占用低** - 不需要运行浏览器
5. **易于集成** - 可以轻松集成到其他系统

### 📁 新文件结构
```
~/Desktop/ApiUsageMonitor/
├── simple_monitor.py          # 主监控脚本（新版本）
├── setup_auth.py              # 认证信息设置脚本
├── 快速启动.py                # 快速启动脚本
├── 新版本使用说明.md          # 本文件
├── auth_info.json             # 认证信息文件（自动生成）
└── usage_cache.json           # 用量数据缓存
```

## 🚀 使用步骤

### 第1步：设置认证信息

#### 方法A：使用快速启动脚本（推荐）
```bash
cd ~/Desktop/ApiUsageMonitor
python3 快速启动.py
```
选择选项1进行设置。

#### 方法B：直接运行设置脚本
```bash
cd ~/Desktop/ApiUsageMonitor
python3 setup_auth.py
```

### 第2步：获取认证信息

#### 获取Authorization Token（推荐）
1. 打开Chrome浏览器
2. 访问 https://v2.aicodee.com/console/topup
3. 按F12打开开发者工具
4. 切换到Network标签
5. 刷新页面
6. 找到API请求（如 `/api/user/info`）
7. 查看请求头中的Authorization
8. 复制值（格式：`Bearer xxxxxxxx`）

#### 获取Cookies
1. 打开Chrome浏览器
2. 访问 https://v2.aicodee.com/console/topup
3. 按F12打开开发者工具
4. 切换到Application标签
5. 展开Cookies
6. 点击 `https://v2.aicodee.com`
7. 复制所有Cookie值

### 第3步：运行监控

#### 方法A：使用快速启动脚本
```bash
cd ~/Desktop/ApiUsageMonitor
python3 快速启动.py
```
选择选项2运行监控。

#### 方法B：直接运行监控脚本
```bash
cd ~/Desktop/ApiUsageMonitor
python3 simple_monitor.py
```

## 📊 输出示例

```
============================================================
API 用量监控工具 - 简化版本
============================================================

使用认证方式: token
尝试 /api/user/info...
✅ 成功获取数据

============================================================
📊 用量信息
============================================================
剩余$28.30（94%），距离额度重置剩余3小时30分。套餐剩余17天，至 5/22/2026, 10:30:26 AM
============================================================
```

## 🔧 常见问题

### 问题1：提示"未找到认证信息"
**解决方法**：
1. 运行 `python3 setup_auth.py` 设置认证信息
2. 确保按照步骤正确获取了Token或Cookies

### 问题2：API返回401错误
**解决方法**：
1. Token可能已过期，需要重新获取
2. 检查Token格式是否正确（应以`Bearer`开头）
3. 检查Cookies是否完整

### 问题3：无法连接到API
**解决方法**：
1. 检查网络连接
2. 确认API地址是否正确
3. 检查是否有防火墙限制

## 💡 使用技巧

### 1. 保存认证信息
认证信息会自动保存到 `auth_info.json` 文件，下次运行时会自动加载。

### 2. 定期更新Token
Token通常有有效期，建议定期检查并更新。

### 3. 集成到其他系统
可以将 `simple_monitor.py` 中的类导入到你的项目中使用。

### 4. 查看历史数据
用量数据会自动保存到 `usage_cache.json` 文件。

## 🎯 推荐工作流程

1. **首次使用**：
   - 运行 `python3 快速启动.py`
   - 选择选项1设置认证信息
   - 选择选项2运行监控

2. **日常使用**：
   - 直接运行 `python3 simple_monitor.py`
   - 或使用快捷方式（见下方）

3. **创建快捷方式**：
   ```bash
   echo 'python3 ~/Desktop/ApiUsageMonitor/simple_monitor.py' > ~/Desktop/check_usage.sh
   chmod +x ~/Desktop/check_usage.sh
   ```

## 📞 需要帮助？

如果遇到问题，请：
1. 检查本使用说明的常见问题部分
2. 确认所有步骤已正确执行
3. 查看脚本输出的错误信息

---

**提示**：新版本更简单、更快、更高效！
